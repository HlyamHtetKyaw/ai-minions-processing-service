# Tests: skip Redis pub/sub listener (no Redis required for contextLoads)
app.processing.consume-jobs=false
# Dummy token so MainServiceWorkerClient does not log ERROR on every context load
app.processing.worker-token=test-worker-token
